# web2video

口播网页转视频工具集。

| 目录 | 说明 |
| --- | --- |
| [`web2video`](./web2video) | 把口播做成网页场景，配 TTS 与时间轴，再按语言导出视频 |

```bash
cd web2video
npm install
npm run dev
```

建议 **Chrome 或 Edge**。开发时必须用 `npm run dev`，本机 Vite 插件会代理千问 TTS、LLM 和 Edge 翻译。
